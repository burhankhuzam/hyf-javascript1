let b1 = 5;
console.log('The value of my variable b1 is: ' + b1);
let b2 = 15;
console.log('The value of my variable b2 is: ' + b2);
let b3 = "Burhan";
console.log('The value of my variable b3 is: ' + b3);
let b4 = "HYF";
console.log('The value of my variable b4 is: ' + b4);

console.log('my variables are a mix inputs of today');

if (b1 === b2) {
    console.log("b1 & b2 are the same type");
} else {
    console.log("b1 & b2 are NOT the same type");
}

if (b1 === b3) {
    console.log("b1 & b3 are the same type");
} else {
    console.log("b1 & b3 are NOT the same type");
}

if (b1 === b4) {
    console.log("b1 & b4 are the same type");
} else {
    console.log("b1 & b4 are NOT the same type");
}

if (b2 === b3) {
    console.log("b2 & b3 are the same type");
} else {
    console.log("b2 & b3 are NOT the same type");
}

if (b2 === b4) {
    console.log("b2 & b4 are the same type");
} else {
    console.log("b2 & b4 are NOT the same type");
}


if (b3 === b4) {
    console.log("b3 & b4 are the same type");
} else {
    console.log("b3 & b4 are NOT the same type");
}